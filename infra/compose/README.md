# Local Compose Notes

The root `docker-compose.yml` is the primary local orchestration entrypoint.

This folder is reserved for later compose overrides, deployment-specific templates, and operational notes.

